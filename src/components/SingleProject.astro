---
const { techs, title, date, description, link } = Astro.props
---
<article class="mx-auto h-52 w-11/12 rounded-lg border-4 bg-santa-fe-500 shadow-lg border-mongoose-200 flex flex-col justify-around items-left hover:scale-105 transition">
  <h2 class="font-semibold text-3xl mx-auto text-mongoose-50">{title}</h2>
  <span class="font-thin text-sm -mt-5 mx-auto text-mongoose-50">{date}</span>
  <p class="text-mongoose-50 text-center text-balance">{description}</p>
  <div class="flex justify-between">
    <aside class="flex gap-2 ml-2">
      {
        techs.map(tech => (
          <img src={tech} class="w-7 h-7" />
        ))
      }
    </aside>
<aside>
  <a href={link} target="_blank" rel="noreferrer noopener" ><img src="redirect.svg" alt="" class="w-7 h-7 mr-1 hover:scale-110 transition">
  </a>
</aside>
  </div>
  </article>
